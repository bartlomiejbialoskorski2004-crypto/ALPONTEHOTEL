// Empty placeholder section. Kept so the #contact anchor in the menu
// still scrolls somewhere; fill with real content in a later iteration.
export default function Contact() {
  return (
    <section
      id="contact"
      aria-label="Contact"
      className="min-h-[30vh] w-full bg-paper"
    />
  );
}
